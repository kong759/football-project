<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mappers.GamelistMapper">


	<select id="selectTeamInfo" parameterType="int"
		resultType="kr.co.sist.football.gamelist.model.dto.TeamInfo">
		SELECT
		id,
		name,
		logo_path logoPath,
		present_num presentNum,
		max_num maxNum,
		uniform_color_code uniformColorCode,
		strength_code
		strengthCode,
		location,
		formation
		FROM
		team_info
		WHERE
		id=#{teamId}
	</select>
	<select id="selectGameInfo" parameterType="HashMap"
		resultType="kr.co.sist.football.gamelist.model.dto.GameInfo">
		SELECT
		id,
		status_code statusCode,
		max_person_num
		maxPersonNum,
		kind,
		game_date gameDate,
		duration,
		description,
		maxnum_team
		maxnumTeam
		FROM
		game_info
		WHERE
		ROWNUM <![CDATA[<=]]>
		#{endNum}
		AND
		#{startNum} <![CDATA[<=]]>
		ROWNUM
	</select>
	<select id="selectGameLocation" parameterType="int"
		resultType="kr.co.sist.football.gamelist.model.dto.GameLocation">
		SELECT
		name,
		location
		FROM
		game_location
		WHERE
		id = #{gameInfoId}
	</select>

</mapper>